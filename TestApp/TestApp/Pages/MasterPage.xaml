<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:local="clr-namespace:TestApp"
			 x:Class="TestApp.MasterPage"
			 Title="Kill me">
	<StackLayout>
		<ListView x:Name="listView" x:FieldModifier="public">
			<ListView.ItemsSource>
				<x:Array Type="{x:Type local:MasterPageItem}">
					<local:MasterPageItem Title="Your Bookings" IconSource="booking.png" TargetType="{x:Type local:BookingsPage}" />
                    <local:MasterPageItem Title="Home" IconSource="settingscog.png" TargetType="{x:Type local:NewMainPage}" />
                    <local:MasterPageItem Title="Account Settings" IconSource="@drawable/accountcirclenopadding.png" TargetType="{x:Type local:AccountSettings}" />
                    <local:MasterPageItem Title="Search for Hotel" IconSource="@drawable/accountcirclenopadding.png" TargetType="{x:Type local:Pages.HotelPage}" />
				</x:Array>
			</ListView.ItemsSource>
			<ListView.ItemTemplate>
				<DataTemplate>
					<ViewCell>
						<Grid Padding="5,10">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="30"/>
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
														<Image Source="{Binding IconSource}" />
							<Label Grid.Column="1" Text="{Binding Title}" />
						</Grid>
					</ViewCell>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</StackLayout>
</ContentPage>